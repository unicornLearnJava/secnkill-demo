<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjpowernode.secnkill.mapper.OrderMapper">

    <insert id="insertOrder" parameterType="com.bjpowernode.secnkill.pojo.Order">
    INSERT INTO t_order(user_id,
    goods_id,
    delivery_addr_id,
    goods_name,
    goods_count,
    goods_price,
    order_channel,
    STATUS,
    create_date,
    pay_date)
    VALUES(#{userId},#{goodsId},#{deliveryAddrId},#{goodsName},
    #{goodsCount},#{goodsPrice},#{orderChannel},#{status},#{createDate},#{payDate})
    </insert>
    <select id="selectById" resultType="com.bjpowernode.secnkill.pojo.Order">
        select * from t_order where goods_id=#{id}
    </select>


</mapper>